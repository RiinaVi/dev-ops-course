filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/*.log

output.logstash:
  hosts: ["localhost:{{ beats_port }}"]

filebeat.config.modules:
  enabled: true
  reload.enabled: false
  path: /etc/filebeat/modules.d/*.yml

setup.ilm.overwrite: true